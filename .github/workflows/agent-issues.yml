name: Agent Issues

on:
  push:
    branches: [master]
  workflow_dispatch:
  issues:
    types: [opened, reopened, labeled]

permissions:
  contents: read

jobs:
  dsh-backlog:
    if: >-
      github.event_name == 'push'
      || github.event_name == 'workflow_dispatch'
      || (github.event_name == 'issues' && contains(fromJSON('["opened","reopened"]'), github.event.action))
      || (github.event_name == 'issues'
        && github.event.action == 'labeled'
        && github.event.label.name == 'agent/dsh-failed')
    uses: Ornn8/dsh-agent-automation/.github/workflows/dispatch-backlog.yml@beb99e7acc1e42a168cbfb3c2cbdc42d7a8eb2de
    with:
      repository: ${{ github.repository }}
      controller_sha: beb99e7acc1e42a168cbfb3c2cbdc42d7a8eb2de

  dsh-issue:
    if: >-
      github.event_name == 'issues'
      && github.event.label.name == 'agent/dsh'
      && contains(fromJSON('["OWNER","MEMBER","COLLABORATOR"]'), github.event.issue.author_association)
    uses: Ornn8/dsh-agent-automation/.github/workflows/dsh-issue.yml@beb99e7acc1e42a168cbfb3c2cbdc42d7a8eb2de
    with:
      repository: ${{ github.repository }}
      issue_number: ${{ github.event.issue.number }}
      controller_sha: beb99e7acc1e42a168cbfb3c2cbdc42d7a8eb2de
      worker_id: dsh
      runner_labels_json: '["self-hosted","Windows","X64","agent-change"]'

  reconcile-reviews:
    if: github.event_name == 'push'
    uses: Ornn8/dsh-agent-automation/.github/workflows/reconcile-reviews.yml@beb99e7acc1e42a168cbfb3c2cbdc42d7a8eb2de
    with:
      repository: ${{ github.repository }}
      controller_sha: beb99e7acc1e42a168cbfb3c2cbdc42d7a8eb2de
