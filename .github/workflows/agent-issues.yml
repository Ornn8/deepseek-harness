name: Agent Issues

on:
  push:
    branches: [master]
  workflow_dispatch:
  issues:
    types: [opened, reopened, labeled]

permissions:
  contents: read

jobs:
  dsh-backlog:
    if: >-
      github.event_name == 'push'
      || github.event_name == 'workflow_dispatch'
      || (github.event_name == 'issues' && contains(fromJSON('["opened","reopened"]'), github.event.action))
      || (github.event_name == 'issues'
        && github.event.action == 'labeled'
        && github.event.label.name == 'agent/dsh-failed')
    uses: Ornn8/dsh-agent-automation/.github/workflows/dispatch-backlog.yml@21314cce10e0c3e36a710647bc129186d0899e04
    with:
      repository: ${{ github.repository }}
      controller_sha: 21314cce10e0c3e36a710647bc129186d0899e04

  dsh-issue:
    if: >-
      github.event_name == 'issues'
      && github.event.label.name == 'agent/dsh'
      && contains(fromJSON('["OWNER","MEMBER","COLLABORATOR"]'), github.event.issue.author_association)
    uses: Ornn8/dsh-agent-automation/.github/workflows/dsh-issue.yml@21314cce10e0c3e36a710647bc129186d0899e04
    with:
      repository: ${{ github.repository }}
      issue_number: ${{ github.event.issue.number }}
      controller_sha: 21314cce10e0c3e36a710647bc129186d0899e04

  reconcile-reviews:
    if: github.event_name == 'push'
    uses: Ornn8/dsh-agent-automation/.github/workflows/reconcile-reviews.yml@21314cce10e0c3e36a710647bc129186d0899e04
    with:
      repository: ${{ github.repository }}
      controller_sha: 21314cce10e0c3e36a710647bc129186d0899e04
